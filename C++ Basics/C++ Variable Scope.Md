##变量的定义域##

定义域是程序的一个区域，从广义上说有三个可声明变量的区域：
 - Inside a function or a block which is called local variables,
 - 在函数或语句块的内部叫做局部变量。
 - 在函数参数位置定义的叫做形参。
 - 在函数外部定义的叫做全局变量。

随后的章节中会介绍什么是函数以及函数参数的定义。这里先介绍什么是局部、全局变量。


